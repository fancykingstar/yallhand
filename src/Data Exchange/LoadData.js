import {inject, observer, action} from 'mobx-react'
import React from 'react'


// export const LoadChannels = inject("ChannelStore")(
//     observer(({ ChannelStore }) => (  
//     const channels = require('../MockData/Channels.json')
//     const { ChannelStore } = props
//     // ChannelStore.loadChannel(channels)
//     alert("hi")
// )))

// @inject("ChannelStore")
// @observer



// export default function LoadChannels(props) { 
//     const channels = require('../MockData/Channels.json')
//     ChannelStore.loadChannel(channels)
    
// }
