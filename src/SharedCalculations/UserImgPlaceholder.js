export const UserImgPlaceholder = () => {
    return require(`../Assets/Graphics/newuser2.png`)
    //Need to update to ASSOCIATE in UI color to user
    // return require(`../Assets/Graphics/newuser${String(Math.floor(Math.random() * Math.floor(4)))}.png`)
}